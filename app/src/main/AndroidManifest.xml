<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.云湖Old">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 1. 支持外部分享链接 (http/https://yhfx.jwznb.com/share?...) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="yhfx.jwznb.com" />
                <data android:pathPrefix="/share" />
            </intent-filter>

            <!-- 2. 支持自定义 Scheme (yunhu://share, yunhu://add-chat 等) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="yunhu" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".ChatActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".BotProfileActivity" />
        <activity android:name=".GroupProfileActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".WsLogActivity" />
        <activity android:name=".CreateChatActivity" />
        <activity android:name=".AddContactActivity" />
        <activity android:name=".MyPostsActivity" />
        <activity android:name=".MyCollectsActivity" />
        <activity android:name=".BlockedUsersActivity" />
        <activity android:name=".ImagePreviewActivity"
            android:theme="@style/Theme.云湖Old.Fullscreen" />
        <activity
            android:name=".PostDetailActivity"
            android:exported="true">
            <!-- yunhu://post-detail?id=xxx -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="yunhu" android:host="post-detail" />
            </intent-filter>

            <!-- http(s)://www.yhchat.com/c/p/xxx & http(s)://yhchat.com/c/p/xxx -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="www.yhchat.com" />
                <data android:host="yhchat.com" />
                <data android:pathPrefix="/c/p/" />
            </intent-filter>
        </activity>
        <activity android:name=".GroupMembersActivity" />
        <activity android:name=".SectionDetailActivity" />
        <activity android:name=".SectionInfoActivity" />
        <activity android:name=".ArchivedConversationsActivity" />
        <activity android:name=".VideoPlayerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.云湖Old.Fullscreen" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
